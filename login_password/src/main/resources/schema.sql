-- DROP TABLE IF EXISTS  LOGIN;
--
-- CREATE TABLE LOGIN (
--     LOGIN_ID BIGINT GENERATED ALWAYS AS IDENTITY,
--     GUID UUID NOT NULL,
--     USERNAME TEXT NOT NULL,
--     PASSWORD TEXT NOT NULL,
--     DISPLAY_ORIGIN TEXT NOT NULL,
--     ORIGIN TEXT NOT NULL,
--     HOSTNAME TEXT NOT NULL,
--     FORM_ACTION_ORIGIN TEXT NOT NULL,
--     FORM_SUBMIT_URL TEXT NOT NULL,
--     HTTP_REALM TEXT NOT NULL,
--     USERNAME_FIELD TEXT NOT NULL,
--     PASSWORD_FIELD TEXT NOT NULL,
--     TIME_USED INTEGER NOT NULL,
--     CREATE_DATE TIMESTAMP NOT NULL,
--     USED_DATE TIMESTAMP NOT NULL,
--     PASSWORD_CHANGED TIMESTAMP NOT NULL,
--     CONSTRAINT PRI_LOGIN_ID PRIMARY KEY (LOGIN_ID));
